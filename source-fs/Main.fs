namespace Addilo

open FileProcessing
open ActivationFunctions
open NetworkCreation
open NetworkOperations

open System

module Main = 
    
    [<EntryPoint>]
    let main argv =

        //let network = (generateNetwork (sigmoid, [3u; 2u; 2u; 4u], 0.1))

        //printfn "%A" network

        //printfn "%A" (feedForward network [|2.3; 4.3; 3.6|])


        0